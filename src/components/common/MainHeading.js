import { styled, Typography } from "@mui/material";

export const MainHeading = styled(Typography)(() => ({
  fontSize: "1.5rem",
  fontWeight: "700",
  fontStyle: "normal",
  padding: "1rem",
  height: "3.25rem",
}));
